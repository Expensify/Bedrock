name: Publish Expensidev Bedrock Image

on:
  push:
    branches:
      - expensify_prod
  workflow_dispatch:

permissions:
  contents: read
  packages: write

concurrency:
  group: publish-expensidev-bedrock-image
  cancel-in-progress: false

env:
  IMAGE_NAME: ghcr.io/expensify/expensidev-bedrock
  CHECKOUT_TOKEN: ${{ secrets.MELVIN_GH_TOKEN || secrets.CODE_EXPENSIFY_TOKEN || github.token }}

jobs:
  build-amd64:
    name: Build amd64
    runs-on: blacksmith-32vcpu-ubuntu-2404
    timeout-minutes: 90
    steps:
      - name: Checkout Expensidev
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          repository: Expensify/Expensidev
          ref: main
          token: ${{ env.CHECKOUT_TOKEN }}
          path: Expensidev

      - name: Checkout Auth
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          repository: Expensify/Auth
          ref: production
          token: ${{ env.CHECKOUT_TOKEN }}
          path: Expensidev/Auth

      - name: Checkout Bedrock
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          repository: Expensify/Bedrock
          ref: ${{ github.sha }}
          token: ${{ env.CHECKOUT_TOKEN }}
          path: Expensidev/Bedrock

      - name: Checkout FuzzyBot
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          repository: Expensify/FuzzyBot
          ref: production
          token: ${{ env.CHECKOUT_TOKEN }}
          path: Expensidev/FuzzyBot

      - name: Checkout ExpensifyTableManager
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          repository: Expensify/ExpensifyTableManager
          ref: production
          token: ${{ env.CHECKOUT_TOKEN }}
          path: Expensidev/ExpensifyTableManager

      - name: Setup Docker Builder
        uses: useblacksmith/setup-docker-builder@33fed32c1ba8775f20366ec9e8d0cd9fe8fc1dd3

      - name: Login to GitHub Container Registry
        uses: docker/login-action@5e57cd118135c172c3672efd75eb46360885c0ef
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ env.CHECKOUT_TOKEN }}

      - name: Build and push amd64 image
        uses: useblacksmith/build-push-action@8be78d7cc5f972bb9f88f73f3ad84fc59fdd0bf8
        with:
          context: Expensidev
          file: Expensidev/ci/docker/Dockerfile.bedrock
          push: true
          provenance: false
          platforms: linux/amd64
          tags: ${{ env.IMAGE_NAME }}:latest-amd64

  build-arm64:
    name: Build arm64
    runs-on: blacksmith-32vcpu-ubuntu-2404-arm
    timeout-minutes: 90
    steps:
      - name: Checkout Expensidev
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          repository: Expensify/Expensidev
          ref: main
          token: ${{ env.CHECKOUT_TOKEN }}
          path: Expensidev

      - name: Checkout Auth
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          repository: Expensify/Auth
          ref: production
          token: ${{ env.CHECKOUT_TOKEN }}
          path: Expensidev/Auth

      - name: Checkout Bedrock
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          repository: Expensify/Bedrock
          ref: ${{ github.sha }}
          token: ${{ env.CHECKOUT_TOKEN }}
          path: Expensidev/Bedrock

      - name: Checkout FuzzyBot
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          repository: Expensify/FuzzyBot
          ref: production
          token: ${{ env.CHECKOUT_TOKEN }}
          path: Expensidev/FuzzyBot

      - name: Checkout ExpensifyTableManager
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          repository: Expensify/ExpensifyTableManager
          ref: production
          token: ${{ env.CHECKOUT_TOKEN }}
          path: Expensidev/ExpensifyTableManager

      - name: Setup Docker Builder
        uses: useblacksmith/setup-docker-builder@33fed32c1ba8775f20366ec9e8d0cd9fe8fc1dd3

      - name: Login to GitHub Container Registry
        uses: docker/login-action@5e57cd118135c172c3672efd75eb46360885c0ef
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ env.CHECKOUT_TOKEN }}

      - name: Build and push arm64 image
        uses: useblacksmith/build-push-action@8be78d7cc5f972bb9f88f73f3ad84fc59fdd0bf8
        with:
          context: Expensidev
          file: Expensidev/ci/docker/Dockerfile.bedrock
          push: true
          provenance: false
          platforms: linux/arm64
          tags: ${{ env.IMAGE_NAME }}:latest-arm64

  publish-manifest:
    name: Publish multi-arch manifest
    runs-on: blacksmith-2vcpu-ubuntu-2404
    needs: [build-amd64, build-arm64]
    steps:
      - name: Setup Docker Builder
        uses: useblacksmith/setup-docker-builder@33fed32c1ba8775f20366ec9e8d0cd9fe8fc1dd3

      - name: Login to GitHub Container Registry
        uses: docker/login-action@5e57cd118135c172c3672efd75eb46360885c0ef
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ env.CHECKOUT_TOKEN }}

      - name: Publish latest manifest
        run: |
          docker buildx imagetools create \
            --tag "${{ env.IMAGE_NAME }}:latest" \
            "${{ env.IMAGE_NAME }}:latest-amd64" \
            "${{ env.IMAGE_NAME }}:latest-arm64"
